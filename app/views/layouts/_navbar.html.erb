<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="collapse navbar-collapse" id="navbarColor03">
    <ul class="navbar-nav mr-auto">
      <% if current_page?(root_path) %>
        <li class="nav-item active">
          <a class="nav-link" href="/">All maps<span class="sr-only">(current)</span></a>
        </li>
      <% else %>
        <li class="nav-item">
          <a class="nav-link" href="/">All maps</a>
        </li>
      <% end %>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% if current_user.nil? %>
            <%= 'Guest' %>
          <% else %>
            <%= current_user.name %>
          <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
          <% if user_signed_in? %>
            <a class="dropdown-item" href="<%= edit_user_registration_path %>">Edit data</a>
            <a class="dropdown-item" href="<%= destroy_user_session_path %>" data-method="DELETE">Salir</a>
          <% else %>
            <a class="dropdown-item" href="<%= new_user_session_path %>">Sign in</a>
            <a class="dropdown-item" href="<%= new_user_registration_path %>">Sign up</a>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>